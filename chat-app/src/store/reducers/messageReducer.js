const initState = {
	message: [
		{time: '10:14 AM, Today', name: 'Olia', content: 'blah blah blah'}
	]
};

const messageReducer = (state = initState, action) => {
	return state;
};

export default messageReducer;